<p id="notice"><%= notice %></p>

<p>Name : <%= @category.name %></p>
<p>Bezeichnung : <%= @category.label %></p>


<table>
  <tr>
    <th>Nummer</th>
    <th>Name</th>
    <th>Bezeichnung</th>
    <th>Beschreibung</th>
    <th>Marke</th>
    <th>Preis</th>
    <th colspan="3">Optionen</th>
  </tr>
  
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.number %></td>
      <td><%= article.name %></td>
      <td><%= article.label %></td>
      <td><%= truncate(article.description, :length => 30) %></td>
      <td><%= getbrandname(article.brand_id) %></td>
      <td><%= article.price %></td>
      <td><%= link_to 'Show', article %></td>
      <td><%= link_to 'Edit', edit_article_path(article) %></td>
      <td><%= link_to 'Destroy', article, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'Editieren', edit_category_path(@category) %> |
<%= link_to 'Zurück', categories_path %>
